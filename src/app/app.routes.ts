import { Routes } from '@angular/router';
import { ApRoutes } from './shared/consts';

export const routes: Routes = [
  /**{
    path: ApRoutes.cs.value,
    loadChildren: () => import('./complaintsSuggestions/cs.routes')
  } */
];
